# Project: Bayt Alebaa Catalog — Cinematic Onboarding Experience

## Overview
Build a cinematic onboarding experience that plays before the main catalog 
loads. The experience consists of three sequential stages: a branded splash 
screen, a three-video cinematic tour, and a welcome transition screen. The 
goal is to create a premium, immersive "wow" moment that positions the 
catalog as an extension of the physical showroom experience.

## Tech Stack
- React (with TypeScript if possible)
- Tailwind CSS for styling
- Framer Motion for animations
- HTML5 `<video>` element for video playback
- localStorage to track first-time visitors

## Project Structure
```
src/
  components/
    onboarding/
      OnboardingFlow.tsx          (main orchestrator)
      SplashScreen.tsx            (Stage 1)
      CinematicTour.tsx           (Stage 2)
      WelcomeTransition.tsx       (Stage 3)
      SkipButton.tsx              (reusable)
    ...
  assets/
    videos/
      ceramic.mp4
      sanitary.mp4
      decorative.mp4
    images/
      logo.svg
  App.tsx
```

---

## Stage 1: Splash Screen (3 seconds)

### Visual Design
- **Background:** Dark gradient (deep navy `#0a1628` to near-black `#050a14`)
- **Floating particles:** 20-30 small circular bubbles floating slowly 
  upward across the screen
  - Color: Brand blue (`#2B6CB0` or match the Bayt Alebaa logo blue) 
    with 30-40% opacity
  - Size: varies between 4px and 12px
  - Animation: Float upward slowly (20-30 seconds per bubble to cross 
    the screen), with subtle horizontal drift
  - Use Framer Motion for smooth continuous animation
- **Logo:** Centered, appears with fade-in + scale animation (0.9 → 1.0)
  over 0.8 seconds
- **Tagline below logo:** Arabic text "حيث يبدأ كل تفصيل"
  - Font: IBM Plex Sans Arabic or Tajawal, weight 300 (light)
  - Color: white with 80% opacity
  - Appears 0.4 seconds after logo with fade-in from below (translate-y 
    10px → 0)
- **CTA Button:** Appears 2 seconds after page load
  - Text: "ادخل المعرض"
  - Style: Outlined button with brand color border, transparent 
    background, hover fills with brand color
  - Icon: Left-pointing arrow (since Arabic is RTL) using Lucide React
  - Animation: fade-in + scale from 0.95 to 1.0

### Interactions
- Clicking "ادخل المعرض" transitions to Stage 2
- If user doesn't click, button stays visible (don't auto-advance)

### Transition Out
- Fade to black over 0.4 seconds before Stage 2 begins

---

## Stage 2: Cinematic Tour (15 seconds)

### Structure
Three videos play in sequence, each 5 seconds long, with text overlays 
and smooth transitions between them.

### Video Playback Component
- Full-screen video element (`object-fit: cover`)
- Videos auto-play, muted, no controls visible
- Use a single `<video>` element and swap sources OR use three `<video>` 
  elements with conditional rendering
- Preload all three videos on Stage 2 mount for smooth transitions

### Video 1 — Ceramic Section (0:00 - 0:05)
- **File:** `ceramic.mp4`
- **Text overlay:** "الجودة تبدأ من اللمسة الأولى"
  - Position: Bottom-center, 15% from bottom edge
  - Font: IBM Plex Sans Arabic or Tajawal, weight 500
  - Size: text-3xl to text-4xl (responsive)
  - Color: white with subtle text-shadow for readability
  - Background: subtle dark gradient behind text (from transparent 
    to black/40%)
  - Animation: fade-in from below at 0:00.5 (translate-y 20px → 0, 
    opacity 0 → 1), fade-out at 0:04.2

### Transition 1 (0:05 - 0:05.3)
- Quick white flash: screen briefly flashes to white (opacity 0 → 0.8 
  → 0) over 0.3 seconds
- Use Framer Motion overlay div with white background

### Video 2 — Sanitary/GROHE Section (0:05 - 0:10)
- **File:** `sanitary.mp4`
- **Text overlay:** "تفاصيل تصنع الفرق"
  - Same styling as Video 1 text
  - Animation: fade-in at 0:05.5, fade-out at 0:09.2

### Transition 2 (0:10 - 0:10.3)
- Same white flash transition

### Video 3 — Decorative Section (0:10 - 0:15)
- **File:** `decorative.mp4`
- **Text overlay:** "إبداع يتحدث بصمت"
  - Same styling as Video 1 text
  - Animation: fade-in at 0:10.5, fade-out at 0:14.2

### Transition Out (0:15 - 0:15.5)
- Fade to white (opacity 0 → 1) over 0.5 seconds before Stage 3

### Skip Button
- Position: Top-left corner (since Arabic UI, top-right might be reserved)
  Actually use top-left for LTR-safe skip placement, OR test both
- Style: Semi-transparent white background with dark text, rounded
- Text: "تخطي" with chevron icon
- Always visible during Stage 2
- Clicking skips directly to Stage 3

---

## Stage 3: Welcome Transition (3 seconds)

### Visual Design
- **Background:** Brand color — warm beige/off-white 
  (`#F5F1EA` or similar warm tone)
- **Logo:** Centered, slightly smaller than Stage 1 (80% size)
  - Fade-in + scale animation (0.95 → 1.0) over 0.6 seconds
- **Welcome text:** Two lines below logo
  - Line 1: "المعرض بين يديك الآن" (larger, weight 600)
  - Line 2: "استكشف بكل حرية" (smaller, weight 300, italic feel)
  - Appears 0.4 seconds after logo with stagger animation
- **CTA Button:** Appears 1.5 seconds after Stage 3 begins
  - Text: "ابدأ الجولة"
  - Style: Filled button with dark brand color background, white text
  - Icon: Left-pointing arrow (RTL)
  - Hover: subtle scale 1.02

### Interactions
- Clicking "ابدأ الجولة" transitions to main catalog
- Fade to white/catalog background over 0.4 seconds

---

## localStorage Logic
```typescript
const ONBOARDING_KEY = 'baytalebaa_onboarding_seen';

// On app mount, check if user has seen onboarding
const hasSeenOnboarding = localStorage.getItem(ONBOARDING_KEY) === 'true';

// If true → skip onboarding entirely, go to catalog
// If false/null → show onboarding

// When user completes or skips onboarding
localStorage.setItem(ONBOARDING_KEY, 'true');
```

Add a developer-only "Reset Onboarding" button in the catalog footer 
(for testing) that clears localStorage and reloads.

---

## RTL Support

- Set `dir="rtl"` on the HTML or root container
- Use Tailwind RTL utilities (`rtl:` prefix) where needed
- Arrows in buttons should point LEFT (since user moves forward = left 
  in RTL)
- Text alignment: right by default for Arabic content

---

## Responsive Design

- Mobile-first approach
- Videos should maintain full-screen on mobile (object-cover)
- Text sizes scale: text-2xl on mobile, text-3xl on tablet, text-4xl 
  on desktop
- Logo scales: 80px on mobile, 120px on desktop

---

## Performance Considerations

- Lazy-load videos (don't load until Stage 2 is triggered)
- Compress videos to web-optimized MP4 (H.264, max 2MB each)
- Add `poster` attribute to video elements with first-frame image for 
  instant visual feedback
- Preload next video while current one plays

---

## Accessibility

- Add `aria-label` to skip button
- Respect `prefers-reduced-motion` media query:
  - Disable particle animations
  - Reduce or remove video auto-play
  - Simplify transitions to basic fades
- Ensure buttons are keyboard-accessible

---

## Deliverables

1. Complete `OnboardingFlow.tsx` component that orchestrates all 3 stages
2. Individual stage components (`SplashScreen`, `CinematicTour`, 
   `WelcomeTransition`)
3. Reusable `SkipButton` component
4. Integration example in `App.tsx` showing how to conditionally render 
   onboarding based on localStorage
5. All animations using Framer Motion
6. Clean, commented TypeScript code

---

## Important Notes

- **No music/audio** in this version (will be added later)
- Videos are muted by default
- All timings are approximate — fine-tune for smooth feel
- Use `AnimatePresence` from Framer Motion for clean mount/unmount 
  animations between stages
- Keep code modular and easy to modify (timings, text, colors should 
  be in constants at top of each component)

---

## Color Palette (approximate — adjust to match Bayt Alebaa brand)
```css
--brand-blue: #2B6CB0;
--brand-blue-light: #4A90D9;
--dark-bg: #0a1628;
--warm-beige: #F5F1EA;
--warm-beige-dark: #E8DFD0;
--text-dark: #1a1a1a;
--text-light: #ffffff;
```

---

## Expected User Experience

1. User opens the website
2. Dark splash screen appears with floating bubbles and logo
3. User reads "حيث يبدأ كل تفصيل" and clicks "ادخل المعرض"
4. Full-screen cinematic tour begins — 3 videos back-to-back with 
   smooth transitions and bilingual-friendly Arabic text overlays
5. Tour ends with warm welcome screen
6. User clicks "ابدأ الجولة" and enters the catalog
7. On next visit, onboarding is skipped automatically